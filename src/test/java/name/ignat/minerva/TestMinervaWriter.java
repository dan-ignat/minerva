package name.ignat.minerva;

/*
 * Unlike MinervaReader, MinervaWriter is straightforward, without much branching, so it should be covered entirely by
 * any of the Test*EndToEnd classes, so there's no need to duplicate coverage here.
 */
public class TestMinervaWriter
{
}
